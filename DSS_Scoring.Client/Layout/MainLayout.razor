@inject NavigationManager NavigationManager

@using static Microsoft.AspNetCore.Components.NavigationManager

@inherits LayoutComponentBase

<div class="page">
    <div class="top-row">
        <div class="button-row">
            <Button  Color="ButtonColor.Primary" TooltipTitle="Proyectos" TooltipPlacement="TooltipPlacement.Bottom" @onclick="LoadProyectos"><Icon Name="IconName.Archive" Size="IconSize.x6" /></Button>
            <Button Class="button-row button" Color="ButtonColor.Primary" TooltipTitle="Alternativas" TooltipPlacement="TooltipPlacement.Bottom" @onclick="LoadAlternativas"><Icon Name="IconName.CardChecklist" Size="IconSize.x6" /></Button>
            <Button Class="button-row button" Color="ButtonColor.Primary" TooltipTitle="Criterios" TooltipPlacement="TooltipPlacement.Bottom" @onclick="LoadCriterios"><Icon Name="IconName.CardList" Size="IconSize.x6" /></Button>
            <Button Class="button-row button" Color="ButtonColor.Primary" TooltipTitle="Matriz" TooltipPlacement="TooltipPlacement.Bottom" @onclick="LoadMatriz"><Icon Name="IconName.FileEarmarkSpreadsheet" Size="IconSize.x6" /></Button>
            <Button Class="button-row button" Color="ButtonColor.Primary" TooltipTitle="Calcular Score" TooltipPlacement="TooltipPlacement.Bottom" @onclick="LoadScore"><Icon Name="IconName.Clipboard" Size="IconSize.x6" /></Button>
        </div>
    </div>
</div>

<article class="content px-4">
    @Body
</article>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>

@code{

    private void LoadLanding()
    {
        NavigationManager.NavigateTo("/");
    }

    private void LoadProyectos()
    {
        NavigationManager.NavigateTo("/Pages/Proyectos");
    }
    private void LoadAlternativas()
    {
        NavigationManager.NavigateTo("/Pages/Alternativas");
    }
    private void LoadCriterios()
    {
        NavigationManager.NavigateTo("/Pages/Criterios");
    }
    private void LoadMatriz()
    {
        NavigationManager.NavigateTo("/Pages/Matriz");
    }
    private void LoadScore()
    {
        NavigationManager.NavigateTo("/Pages/Score");
    }

}

    